import 'package:flutter/material.dart';

class ArchersPage extends StatelessWidget {
  const ArchersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("ExportPage");
  }
}
